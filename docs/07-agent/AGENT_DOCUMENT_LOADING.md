# Agent Document Loading

## Sempre

`AGENTS.md`, constituição, master spec, status e update matrix.

## Por domínio

- Python: skills Python + architecture/quality;
- SQLite: schema, migration, runtime compatibility;
- Kodi UI: routing, playback, compatibility;
- provider: contract, provider spec, network/security;
- release: build, repository, integrity e rollback.

Evitar carregar todos os documentos indiscriminadamente quando a tarefa é pequena, mas nunca omitir os contratos diretamente afetados.
