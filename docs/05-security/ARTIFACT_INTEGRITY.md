# Artifact Integrity

Artefatos são gerados por ferramenta versionada. O pipeline registra versão, commit, lista de arquivos e SHA-256. ZIP não contém `.env`, `.git`, caches, testes secretos, dumps, banco pessoal, logs, ferramentas de desenvolvimento ou credenciais.
