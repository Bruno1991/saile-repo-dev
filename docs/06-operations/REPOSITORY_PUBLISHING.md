# Repository Publishing

O repositório Kodi deve manter `addon.xml` do repository, `addons.xml`, checksum exigido pela implementação, ZIPs versionados e assets. Arquivos gerados não são editados à mão. GitHub Pages publica somente conteúdo necessário. O token usado no push não entra em log nem em URL remota persistida.
