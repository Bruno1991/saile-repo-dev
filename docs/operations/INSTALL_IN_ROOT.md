# Instalação na raiz do repositório

O ZIP `srepo_estrutura_completa_raiz.zip` foi montado sem uma pasta externa. Extraia-o
diretamente em:

```text
C:\Users\conta\OneDrive\Documents\saile-repo-dev
```

Ao confirmar a mesclagem:

- preserve o `.env` existente;
- preserve `.vscode/`;
- remova as duas pastas antigas `srepo_agent_skills_mega_library/` depois de conferir a cópia;
- confirme que `.agents/`, `.github/`, `addons/`, `artwork/`, `docs/`, `tests/` e `tools/` estão na raiz.

O pacote não contém `.env` nem `.vscode/`.
